# GPT-Image

基于 OpenAI GPT-Image-2 API 的 Electron 桌面应用。

## 使用

```bash
# 1. 初始化运行目录（首次使用）
cp -r run-example run

# 2. 编辑 run/.env，填入你的 API Key

# 3. 启动
npm start
```

`run/` 目录已在 `.gitignore` 中，配置不会误提交。

## 开发

```bash
npm run dev
```
